# 報價表模板更新需求

## 文件位置
`public/templates/quotation.docx`

## 需要更新的內容

### 1. 新增「其他報價方式說明」欄位
**位置**：報價方式選項附近
**變數名稱**：`{quoteMethodOther}`
**說明**：當報價方式選擇「其他」時，顯示此欄位的內容

### 2. 簽署欄改為三欄
**原有**：兩欄（索取報價人、科組負責人）
**更新為**：三欄（索取報價人、科組負責人、批核人）

**新增變數**：
- `{approverName}` - 批核人姓名
- `{approverRank}` - 批核人職級
- `{approverDate}` - 批核人日期

**佈局建議**：
```
| 索取報價人          | 科組負責人          | 批核人             |
|---------------------|---------------------|--------------------|
| （簽署空白區）      | （簽署空白區）      | （簽署空白區）     |
| {requestorName}     | {deptHeadName}      | {approverName}     |
| {requestorRank}     | {deptHeadRank}      | {approverRank}     |
| 日期：{requestorDate}| 日期：{deptHeadDate}| 日期：{approverDate}|
```

### 3. 字體大小檢查
根據 KCquotation 的更新，需要確保模板中所有文字字體大小 ≥ 6pt。
建議統一使用 12pt 避免過小字體問題。

### 4. Checkbox 字符（可選）
如果模板中有 checkbox，建議使用：
- 勾選：`☑` (U+2611) 或字體符號 ``
- 未勾選：`☐` (U+2610)

## 更新後測試
1. 開啟 Word 模板
2. 檢查所有變數名稱與代碼一致
3. 使用「設計」→「版面配置」檢查表格寬度
4. 測試生成：在系統中選擇「其他」報價方式，填寫批核人資料，生成 DOCX 檢查顯示

## 代碼端已完成的更新
✅ API route 支援新欄位
✅ 前端表單加入批核人和其他報價方式
✅ 預覽面板更新為三欄簽署
✅ 日期自動格式化（YYYY-MM-DD → 中文格式）
✅ 所有 state 和 payload 已更新

---
**更新日期**：2026-07-15
**對應 KCquotation commit**：最新版（批核人 + 其他報價方式）
